# Tasks — Padrão Filtro (validação pós-feedback Diogo)

## P0 — Correção fluxo §08

- [x] Separar pipeline de recorte (Período → Busca → Alçada → Drawer) de layout/consumidores (KPIs, gráficos, tabela em paralelo).
- [x] Documentar contadores da alçada (base sem recorte minha/todos).
- [x] Explicitar que ordem da toolbar (UI) ≠ pipeline de dados.
- [ ] Revisão Diogo na página `/docs/patterns/filtro#fluxo`.

## P1 — Consistência doc (feed do dia)

- [x] Hero navy + corpo 1200px (FiltroDemo).
- [x] Cards/tipografia PatternSection + SpecCard.
- [x] Tutorial `pageTutorials.filtro`: passo §08 Fluxo + targets `s(n)` ajustados.
- [x] Data Listing §06 regra toolbar: Alçada entre Filtros e Busca.

## P2 — Anexos Registro (OPA → DS)

- [x] PhotoEvidenceDropzone em Input §10 + Form Workspace card 4.
- [x] LocationPinButtons em Select §01-G + Form Workspace.
- [x] Paridade OPA: `Alert` no erro, `alt` «Foto do OPA», `Lightbox` interno na doc.

## P3 — Release

- [ ] `npm run build` + smoke `/docs/patterns/filtro`.
- [ ] Changelog + semver (somente se pedido commit/release).

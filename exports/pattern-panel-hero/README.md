# PatternPanelHero

Banner de conteúdo (BannerConteudo1/2) com junction lines e chips de KPI opcionais.

Inclui `banner-shared.tsx` (BannerJunctionLines, BannerIconBox).

## CSS obrigatório

Classes `.fips-banner-shell--content` e variáveis `--fips-banner-content-*` — ver `src/styles/globals.css` no repositório DS-FIPS.
